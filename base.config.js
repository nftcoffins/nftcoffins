const baseUrl = 'https://nftcoffins.com/api';
const baseTokenUri = `${baseUrl}/items/`;
const baseContractUri = `${baseUrl}/contract`;
const totalSupplyLimit = 420;

module.exports = {
  baseUrl,
  baseTokenUri,
  baseContractUri,
  totalSupplyLimit,
};
